---
sidebar_position: 3.9
description: 跳房子记号属性的写法
---
import C from '@site/src/mdx'
import * as Cd from '@site/src/mdx/reference/commands'

# 跳房子、升降八度 {#jumper-props}

<Cd.CommandReference level={2} />

如你所见，跳房子记号的属性是在碎片的头部描述的。这一点限制了碎片的划分——一个碎片不能一半在跳房子记号里面，一半在外面；也不能一半在一个跳房子中，另一半在另一个跳房子中。

## 跳房子记号的属性 {#jumper-props-1}

跳房子记号可以有这些属性：

- 迭代数 `<迭代数>.`，例如 `1.` `2.`。
- 自定义文本 `"<文本>"`。
- 升或降八度，例如 `8va` `8vb`。
- 开放区间记号 `*`。

写起来像是这样：

<C.ScoreExample canLoad>{
`P: 1=C 4/4
====
N: 1 5 3 5 | (43)(23) 4 - | (21) (7d1) 2 4 | 3 4 5 - |
---
N: 6. (5) 4 6 | 5 3 1 - |
---
// highlight-next-line
J: 1.
N: (43)(21)(7d1)(26) | 5 2 5 - :||
---
// highlight-next-line
J: 2., "结束句"
N: (43)(21)(7d1) (27d) | 1 3 1 - |||
`}</C.ScoreExample>

开放区间记号适用于特别长的跳房子区间。它不会显示，但是若跳房子属性中包含它，则 Sparks NMN 只会显示跳房子记号最前面和最后面的一段线，省略中间奇长无比的那一段横线。不过，如果跳房子区间很短，以至于起点和终点在同一行里面，这是不会起作用的。

## 跨越多个碎片的跳房子 {#jumper-connect}

若要创建跨越多个碎片的跳房子记号，可以让这些碎片的跳房子属性完全一致（包括顺序），它们的跳房子记号会被连在一起。

&#xFEFF;<C.MinVersion min='1.15.6' inline>为简单起见，也可以从第二个碎片开始只写 `J:`，省略之后的属性。</C.MinVersion>

## 移动小节序号 {#shift-sectionorder}

当某行的开头恰好是一个跳房子记号的开头时，小节序号会自动向左移动一小段距离，避免与跳房子记号的标注发生重叠。
